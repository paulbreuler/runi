<script lang="ts">
  /**
   * {{ComponentName}} component for {{purpose}}.
   */

  import type { {{PropsType}} } from '$lib/types';

  // Props
  interface Props {
    // Add props here
  }
  let { ...props }: Props = $props();

  // State (runes)
  // Add state here

  // Derived
  // Add derived state here

  // Handlers
  // Add handlers here
</script>

<div class="{{component-name}}">
  <!-- Component content -->
</div>

<style>
  .{{component-name}} {
    /* Styles */
  }
</style>
